    // cout << "3 - Caminos más cortos entre centrales." << endl;

    // pair<int, vector<int>> resultCentrales = tspCentrales(g.matAdj, n, colonias);

    // if (resultCentrales.first != INF) {
    //     // Print the route
    //     for (size_t i = 0; i < resultCentrales.second.size(); i++) {
    //         int idx = resultCentrales.second[i];
    //         cout << colonias[idx].nombre;
    //         if (i != resultCentrales.second.size() - 1)
    //             cout << " - ";
    //     }
    //     cout << " (" << resultCentrales.first << ")" << endl;
    // } else {
    //     cout << "No se encontró una solución factible." << endl;
    // }